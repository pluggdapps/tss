#.adbl-link-cont{text-align:center;}
/* !important suffix for declaration can replace SEMICOLON ? */
#profileNotes dd{
  height:auto !important
  min-height:100px;
}
/* Dangling class extender */
ul.{
	list-style:outside disc;
	margin:2.3em 0 0 4.6em;
}
