.ap_cnep_label{ background:; float: left;width: 120px; text-align:right; vertical-align: bottom;font-weight:bold;}
.brain-games-item br{ /* Empty property value */
  padding0;
}
/* Illegal prefix ($) for simple selector */
.hd-wrap,$gametap .hd,#gametap{background:none;}

/* Illegal prefix (#) (.) for property name */
#footerWrapper #footer .legal { #padding-top : 15px; }
#footerWrapper #footer .legal { .padding-top : 15px; }

/* Argument to function alpha contains ':' operator */
a { filter: alpha(opacity:100); }

/* Is this allowed ? atleast W3C CSS grammar does not allow */
td {
  padding:1px;!important;
}

/* `any` rule found inside ruleset grammer [if IE 7] */
.cnn_twc1_large { margin: 15px 0px; [if IE 7] margin-bottom: 10px; }
